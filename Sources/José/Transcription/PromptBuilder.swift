import Foundation

// Worker B3 (feat/transcription) owns this file — replaces the inline
// PromptBuilder stub in TranscriptionClient.swift with a real version
// that loads DefaultVocabulary.yaml at launch and composes the prompt
// with the user's selected categories + custom terms.
